static_resistance_calc(voltage, current)
{
	resistance=voltage/current;
	return resistance;
}

differential_resistance_calc()
{
	//uh this is gonna get cray cray.
	
	//so R_diff=dV/dI
	
	//but I don't know the function to take the derivative.
	
	//so we need to figure out how to write a program that can define a component.
}